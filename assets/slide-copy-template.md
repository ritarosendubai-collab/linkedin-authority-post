# Slide Copy Template

Fill this before rendering anything. If the copy does not work in plain text, no amount of design will save it.

Canvas 1080 × 1350. Body type minimum ~36px. Article reference in a consistent position on every claim slide.

---

## 01 — Cover

**Hero:** `{{number_or_countdown}}`
**Sub:** `{{consequence_in_one_line}}`
**Small:** `{{topic_line}}`
**Masthead:** `{{issuer}}` *(check emblem rights)*

> Lead with the stake, not the subject. The topic is orientation; the number is the headline.

## 02 — Context

**Head:** What changes
**Body:** `{{issuer}}` issued `{{instrument_number}}` in `{{publication_month}}`. It takes effect `{{effective_date}}`.
**Ref:** `{{article_ref}}`

## 03–06 — The protections

One per slide. Repeat this block.

**Head:** `{{protection_headline}}`
**Body:** `{{one_sentence_of_detail}}`
**Ref:** `{{article_ref}}`

> Numerals, not words. One obligation per slide. If it needs two sentences, it is two slides.

## 07 — The turn

**Head:** Here's the part most people miss
**Body:** `{{qualifier_in_plain_language}}`
**Ref:** `{{article_ref}}`

> This is the spine of the deck. It should be the slide someone screenshots.

## 08 — What that means

**Head:** `{{consequence_headline}}`
**Body:** `{{who_bears_the_risk_and_why}}`
**Ref:** `{{article_ref}}`

## 09 — Readiness checklist

**Head:** Before `{{effective_date_short}}`
**Body:** `{{item_1}}` · `{{item_2}}` · `{{item_3}}` · `{{item_4}}`
**Callout:** `{{the_item_most_applications_fail_on}}`

## 10 — Considered close *(optional)*

**Body:** `{{tone_calibrated_line}}`

> Chosen by the human from three temperatures. Never auto-selected.

## 11 — Closing

**Question:** `{{binary_question}}`
**Source:** `{{instrument_full_name}}`, `{{instrument_number}}` — `{{source_domain}}`
**Contact:** `{{handle_or_site}}`
**Visual:** treated crop of the cover image, to bookend the deck.

---

## Pre-render checklist

- [ ] Every claim slide carries its article reference
- [ ] No slide holds more than one obligation
- [ ] Body copy fits at ≥36px without shrinking
- [ ] Cover hero is a stake, not a topic
- [ ] Qualifier is present and prominent
- [ ] Exactly one call to action, on the last slide
- [ ] Image rights confirmed for every asset
